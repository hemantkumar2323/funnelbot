import React from 'react'
import ReactDOM from 'react-dom/client'
import FunnelBot from './FunnelBot.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FunnelBot />
  </React.StrictMode>
)
